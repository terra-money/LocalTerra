services:
  terrad:
    extends:
      file: compose-rpc.yml
      service: terrad

  postgres:
    extends:
      file: compose-fcd.yml
      service: postgres

  fcd-collector:
    extends:
      file: compose-fcd.yml
      service: fcd-collector

  fcd-api:
    extends:
      file: compose-fcd.yml
      service: fcd-api

volumes:
  localterra:
  pgdata:

networks:
  localterra:
